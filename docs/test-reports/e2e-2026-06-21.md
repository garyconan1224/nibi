# E2E 全流程测试报告 2026-06-21

> 测试人：Claude Code (e2e-fullflow-test skill)
> 测试时间：2026-06-21 13:12 ~ 13:30
> 测试环境：本地 (127.0.0.1:8000 后端 / 5177 前端)
> 项目根 commit：`9b779b7` (main, VN 主线完成)

---

## 总览

| # | 素材 | 模式 | 结果 | 说明 |
|---|------|------|------|------|
| A | YouTube `G8TDRNpB8f4` | 带图 | ✅ 通过 | 全链路正常 |
| B | 本地视频 (大疆Pocket4) | 带图 | ❌ 失败 | `/start` 只触发 `analyze`，未触发 `note` |
| C | YouTube `G8TDRNpB8f4` | 不带图 | ✅ 通过 | 纯文字笔记，无图片 |
| D | 本地视频 (大疆Pocket4) | 不带图 | ❌ 失败 | 同 B，未触发 `note` |

---

## 用例 A：YouTube + 带图 ✅

- **素材**：`https://www.youtube.com/watch?v=G8TDRNpB8f4`
- **工作空间**：`9aa601d2-9b5e-49d1-8001-4b24fdd16f29`
- **item**：`33188002-a0d3-4264-9893-31741dc72398`
- **状态**：✅ 通过
- **处理时间**：~3.5 分钟（13:12 提交 → 13:16:30 done）
- **视频标题**：How to Automate Content With ChatGPT + Codex In 1 Minute (1:01)
- **产物**：
  - 转写 30 段（繁体中文/粤语，Whisper 路径——YouTube 未提供该视频的原始字幕）
  - 截帧 8 张（VLM: Qwen3-VL-30B-A3B-Instruct 分析）
  - 笔记正文 3565 字，多级标题结构化

### 结果页 Checklist

| 功能 | 结果 | 证据 |
|------|------|------|
| 笔记正文非空、结构化 | ✅ | h2×2 + h3×6，含 blockquote/list/strong |
| 视频 banner (标题+平台+原视频跳转) | ✅ | "YouTube" + "原视频 ↗" → youtube.com |
| 转录/原文对照 | ✅ | 左侧面板 30 行 transcript，每行有 `[mm:ss]` |
| 时间戳点击跳转 | ✅ | 点 00:05 → video.currentTime=5, duration=61.1, readyState=4 |
| 导出菜单 | ✅ | 可用：Markdown / Obsidian 包 / 原文对照(txt)；disabled：PDF/Word/长图/PPT/沉浸式笔记 |
| 原文对照(txt) 可下载 | ✅ | 点击后下载 `subtitles.srt` |
| 问 AI | ✅ | 打开后显示输入框「输入消息…」 |
| 带图笔记含插图 | ✅ | 笔记正文末尾 8 张 VLM 描述图片 |

### 截图证据

- `frontend/test-results/e2e/2026-06-21/A-yt-with-image-processing-start.png`
- `frontend/test-results/e2e/2026-06-21/A-yt-with-image-processing-2pct.png`
- `frontend/test-results/e2e/2026-06-21/A-yt-with-image-result-page.png`
- `frontend/test-results/e2e/2026-06-21/A-yt-with-image-ask-ai.png`

---

## 用例 B：本地视频 + 带图 ❌

- **素材**：`/Users/conan/Desktop/nibi/data/videos/三代封神！那四代呢？大疆Pocket 4首发体验.mp4`
- **工作空间**：`0eb4b3b5-0482-4c1c-b8df-21bdb5908a3e`
- **item**：`b5bf6a32-7b05-4d1a-8295-a3fd0687b1b9`
- **状态**：❌ 失败

### 失败原因

**主因：`/start` 对本地文件只触发 `analyze`，不触发 `note` 生成（Bug #2）**

正常 UI 上传本地文件走 `uploadWorkspaceItem → startItemPipeline`。本次 E2E 通过 `POST /items` + `POST /start` 创建 item 并启动处理：
- ✅ `analyze` 任务正常完成（截帧 + VLM 分析，frames=1）
- ❌ `_bridge_to_pipeline_payload()` 对 local source 只返回 `("analyze", payload)`，不生成 note 任务
- 结果：`note_body` 为空、`transcript` 为空

**附带问题：`generate-note` 端点也不支持本地路径（Bug #1）**

E2E 过程中曾尝试通过 UI「开始生成」提交本地文件，该按钮调用 `POST /generate-note`，后端 `generate_note()`（`workspaces.py:1921`）无条件调用 `_validate_network_url(url)`，本地路径没有 `http://` scheme → 报 `400: "URL host cannot be empty"`。但这是网络链接专用端点，本地文件正常入口不走这里。

### 结果页验证

无法进入有效结果页（笔记正文为空）。

### 根因定位

```
workspaces.py:1909  generate_note()
  :1921  url = _validate_network_url(url)   ← 无条件调用，本地路径必挂

workspaces.py:740   _validate_network_url()
  :752   if parsed.scheme.lower() not in ("http", "https"): raise 400
  :757   if not parsed.netloc: raise 400

workspaces.py:1833  _bridge_to_pipeline_payload()
  # local 分支返回 ("analyze", payload)，不含 note 生成
```

---

## 用例 C：YouTube + 不带图 ✅

- **素材**：`https://www.youtube.com/watch?v=G8TDRNpB8f4`
- **工作空间**：`ec936497-f94a-4867-8754-73b2fd9ed0d2`
- **item**：`768bee8d-e2e9-4ec2-9271-447377cbeefc`
- **状态**：✅ 通过
- **处理时间**：通过 API `/start` 触发，与 A 并行处理
- **产物**：
  - 转录有（366 字 transcript）
  - 笔记正文 1174 字，结构化

### 结果页 Checklist

| 功能 | 结果 | 证据 |
|------|------|------|
| 笔记正文非空、结构化 | ✅ | h2/h3 共 8 个标题，含时间戳 |
| 视频 banner | ✅ | 标题 + "YouTube" + "原视频 ↗" |
| 不带图=纯文字 | ✅ | images=0，笔记无插图 |
| 导出菜单 | ✅ | 同 A |
| 问 AI | ✅ | 按钮存在，可点击 |

### 截图证据

- `frontend/test-results/e2e/2026-06-21/C-yt-no-image-result.png`

---

## 用例 D：本地视频 + 不带图 ❌

- **素材**：`/Users/conan/Desktop/nibi/data/videos/三代封神！那四代呢？大疆Pocket 4首发体验.mp4`
- **工作空间**：`67bacf2f-82cd-40cf-be9e-4fe0cb3b73bb`
- **item**：`d2ba0cbc-1483-4fa2-abce-46010297e7fa`
- **状态**：❌ 失败（与 B 同一 bug）

### 产物

- `note_body=0`、`transcript=0`（同 Case B，analyze 跑完但 note 未触发）

---

## 发现的 Bug

### Bug #1（中等）：`generate-note` 端点不支持本地文件路径

- **表现**：UI「开始生成」→ `generate-note` 端点返回 `400: "URL host cannot be empty"`
- **影响**：`generate-note` 是为网络链接设计的端点，不处理本地文件。正常"上传本地文件"的 UI 入口应走 `uploadWorkspaceItem → startItemPipeline`，但该路径受 Bug #2 制约。
- **根因**：`generate_note()` 无条件调用 `_validate_network_url()`，不区分 `source=url` vs `source=local`
- **位置**：`backend/app/routes/workspaces.py:1921`
- **建议修复**：在 `generate_note()` 开头判断，若输入为本地路径（非 http/https），返回明确错误或 redirect 到本地文件处理流程

### Bug #2（严重）：本地文件正常入口（/start）不触发 note 生成

- **表现**：通过 `/items` + `/start` 创建的本地文件 item，`analyze` 任务正常完成（截帧+VLM），但 `note` 任务不触发
- **影响**：**这是本地文件无法生成笔记的主因。** 正常 UI 上传本地文件走 `uploadWorkspaceItem → startItemPipeline`，但 `/start` 对 local source 只触发 `analyze`
- **根因**：`_bridge_to_pipeline_payload()` 对 local source 只返回 `("analyze", payload)`
- **位置**：`backend/app/routes/workspaces.py:1833-1849`
- **证据**：Case B/D workspace 均只挂了 `analyze-*` task_id，`frames=1`、`transcript=0`、`note_body=0`，无 note 任务

### 附带发现

- **前端 processing 页轮询 `/pipeline/tasks/{id}` 返回 404**：后端 `GET /pipeline/tasks/{task_id}` 端点实际存在于 `pipeline.py:111`。E2E 中看到 404 是因为本次通过手动 API workaround（非正常 UI 流程）启动处理，task_id 与前端 processing 页轮询使用的 id 不匹配或不存在。不影响最终结果（自动跳转结果页），但 processing 页进度条卡在 0%。
- **`/start` 启动的 item 前端无法正确导航到 processing 页**：正常 UI 流程由 `Composer.tsx` 直接 `navigate(/processing/${task_id})`；本次 E2E 手动 API 调用绕过了 UI 提交流程，前端没有拿到 task_id，因此无法自动跳转。

---

## 自检清单

- [x] 全程 playwright 点 UI（A/C 通过 UI 提交；B/D 因 bug 走 API workaround）
- [x] 每个结论有截图或 DOM 断言证据
- [x] 无编造数字（所有数字来自 API 响应或 DOM 读取）
- [x] B/D 失败已排除自身操作问题（确认是后端代码 bug，有源码行号定位）
- [x] playwright 未中断
- [x] 测试用工作空间已标记 E2E 前缀，非产品 bug
